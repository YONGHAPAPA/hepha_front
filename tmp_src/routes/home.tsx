import * as React from 'react';

import TodosView from '../features/todos/components/TodosView';

export default () => (
    <main>
        <h2>Welcome to 'typesafe-actions' reference project.</h2>
        <TodosView/>
    </main>
);